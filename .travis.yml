language: cpp
matrix:
    include:
        - os: linux
          dist: bionic
          compiler: gcc
        
before_install:
  - sudo apt-get install -y libeigen3-dev

script: 
    - mkdir -p build
    - cd build
    - cmake ..
    - make -j
